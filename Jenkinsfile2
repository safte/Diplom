pipeline {
    agent any
    options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
    }
    stages {
        stage('Pull image from Dockerhub') {
            steps {
              withDockerRegistry([ credentialsId: "dockerhub", url: "https://hub.docker.com/repository/docker/dhwe/appy" ]) {
                sh 'make pull'
              }
            }
        }
        stage('Run image') {
            steps {
                sh 'make run'
            }
        }
    }
}
